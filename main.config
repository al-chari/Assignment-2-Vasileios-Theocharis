debug = false

// Process scope mode is set to copy making the outputs copied not moved, path sets a specific directory specifically the output directory

param.outdir="output"

process{
  publishDir{
    path= "${param.outdir}
    mode="copy"
  }
}
